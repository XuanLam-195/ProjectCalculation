spring.application.name=ProjectCalculation

DB_HOSTNAME=jdbc:h2:mem:testdb;MODE=MYSQL;DATABASE_TO_LOWER=TRUE;INIT=runscript from 'classpath:h2schema.sql';
DB_USERNAME=sa
DB_PASSWORD=

spring.datasource.url=${DB_HOSTNAME}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}

spring.datasource.driverClassName=org.h2.Driver